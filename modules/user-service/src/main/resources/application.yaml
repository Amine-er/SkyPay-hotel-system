spring:
  config:
    import: "configserver:"
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8888
      name: user-service
      fail-fast: false
      retry:
        initial-interval: 3000
        multiplier: 1.3
        max-interval: 10000
        max-attempts: 20