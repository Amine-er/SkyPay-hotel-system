spring:
  config:
    import: "configserver:"
  application:
    name: payment-service-mock
  cloud:
    config:
      uri: http://localhost:8888
      name: payment-service-mock
      fail-fast: false
      retry:
        initial-interval: 3000
        multiplier: 1.3
        max-interval: 10000
        max-attempts: 20