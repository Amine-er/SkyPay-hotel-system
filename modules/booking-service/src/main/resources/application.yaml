spring:
  config:
    import: "configserver:"
  application:
    name: booking-service
  cloud:
    config:
      uri: http://localhost:8888
      name: booking-service
      fail-fast: false
      retry:
        initial-interval: 3000
        multiplier: 1.3
        max-interval: 10000
        max-attempts: 20