apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: skypay-hotel-db-pvc
  namespace: dev
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi